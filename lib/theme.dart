/// Colores y constantes de diseño de ApliBot.
/// El bot puede editar este archivo para cambiar el look de la web.
import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF005FA9);
  static const primaryDark = Color(0xFF00467B);
  static const accent = Color(0xFFE8955E);
  static const accentDark = Color(0xFFB46734);
  static const lightBlue = Color(0xFF8FD5FA);
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const background = Color(0xFFFDFDFD);
  static const backgroundSoft = Color(0xFFF4F5F7);
  static const text = Color(0xFF303030);
  static const textMuted = Color(0xFF6B7280);
}
